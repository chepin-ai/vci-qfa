name: key-sentinel-01
# KEY-SENTINEL-LINE-01 重装(qfa 犬粮先装, RCA-KEYHOLE-01§机件化 + KEY-PETITION-RCA-02§F5)
# CI-ZERO: 纯事件驱动——唯 workflow_dispatch/repository_dispatch, 无定时器
on:
  workflow_dispatch:
  repository_dispatch:
    types: [key-scan, federation-event]
permissions:
  contents: write
jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 6
    steps:
      - uses: actions/checkout@v4
      - name: key-scan
        env:
          AI_FULL_PAT: ${{ secrets.AI_FULL_PAT }}
          CI_OPS_LINE_KEY: ${{ secrets.CI_OPS_LINE_KEY }}
          GH_PAT_QI_FULL: ${{ secrets.GH_PAT_QI_FULL }}
          LINE_PAT: ${{ secrets.LINE_PAT }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python3 ci/key_sentinel.py
      - name: commit receipt
        run: |
          git config user.name "qfa-si5"
          git config user.email "qfa-si5@users.noreply.github.com"
          git add receipts/key-sentinel/ || true
          git diff --cached --quiet || git commit -m "key-sentinel receipt [skip ci]"
          git push
